version: "3.9"

services:
  postgres:
    container_name: jello_database
    image: postgres:16
    restart: always

    ports:
      - "5432:5432"

    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=jello_app